# Security P0 — 2026-08-15

## Code changes (this PR)

- Admin-only mutations: payroll payout mark/clear, agent-statements create/patch, BOB patch/upload/rebuild/policy-status, commission/BSI/MedicarePro/agency-production uploads, agency override status, parser fix helpers
- Parameterized BOB agent `ILIKE` filters (no string interpolation of JWT name)
- Safe upload filenames (UUID-style, no client path segments)
- Removed temp `cleanup-upload-374` endpoint
- Boot full-table agent normalize is **off** unless `RUN_STARTUP_NORMALIZE=true`
- Login rate limit (10 / 15 min / IP+email); min password length 8
- Health check pings Postgres
- Scrubbed hardcoded DB URLs / JWT / temp passwords from ops scripts; require `DATABASE_URL`
- Stop tracking `.env` (was committed with weak JWT secret)

## You must still rotate in Railway / Netlify (manual)

Secrets were in git history — scrubbing the tree is not enough.

1. **Rotate Railway Postgres password** (and update `DATABASE_URL`)
2. **Set a new strong `JWT_SECRET`** on Railway (invalidates all sessions)
3. **Reset Yahoska/Katy (and any) account passwords** if they ever matched values that appeared in scripts
4. Redeploy API after merge

Until rotation, treat the old DB password + JWT secret as compromised.
