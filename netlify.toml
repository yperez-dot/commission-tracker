[build]
  command = "DISABLE_ESLINT_PLUGIN=true CI=false npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so /payroll-payouts (and other in-app routes) serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
